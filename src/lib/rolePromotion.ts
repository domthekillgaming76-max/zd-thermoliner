/** @deprecated Import from ./roles instead — kept for backward compatibility */
export {
  ROLE_PROMOTION_CHAIN,
  type PromotableRole,
  normalizeRoleKey,
  getRoleLabel,
  getNextPromotionRole,
  canManageRolePromotions,
  getPromotionButtonLabel,
} from './roles';
