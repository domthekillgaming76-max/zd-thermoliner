# Client (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never expose in frontend bundle
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=

# Z&D Thermoliner Client (Windows) — API /api/client/updates
ERP_VERSION=2.6.1
CLIENT_LATEST_VERSION=1.0.0
CLIENT_DOWNLOAD_URL=
CLIENT_CHANGELOG=Version initiale du client Z&D Thermoliner
CLIENT_UPDATE_MANDATORY=false

# Coolify scheduled task (every 30 min):
# Command: node scripts/generate-freight-cron.js
# Or: npm run cron:freight
